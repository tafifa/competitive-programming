#include <iostream>
#include <algorithm>
using namespace std;

#define fastIO ios_base::sync_with_stdio(false); cin.tie(NULL);

int main() {
  fastIO;
  
  int n; cin >> n;

  int a[n]; 
  for (int i = 0; i < n; i++) cin >> a[i];
  
  

  return 0;
}