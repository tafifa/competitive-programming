#include <iostream>
using namespace std;

int main() {
  int m,n;
  cin >> m >> n;

  string mtx[n];

  for (int i = 0; i < n; i++) {
    cin >> mtx[i];
  }
  for (int i = 0; i < n; i++) {
    if (s.find('X') != std::string::npos) cout << i << ""
  }

  return 0;
}