#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
  int N;
  vector<int> V;
  for (int i = 0; i < N; i++) {
    cin >> V[i];
  } sort(V.begin(), V.end());
  

  return 0;
}