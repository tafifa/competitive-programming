#include <iostream>
#include <math.h>
#include <string>
using namespace std;

#define ll unsigned long long

int main() {
	int n; cin >> n;

  string stri =  to_string(n);

  char* strChar = stri.c_str();

  return 0;
}