#include <iostream>
using namespace std;

#define fastIO ios_base::sync_with_stdio(false); cin.tie(NULL);

int main() {
  fastIO;

  int n; cin >> n;
  int an[n]; for (int i = 0; i < n; i++) cin >> an[i];
  int bn[n]; for (int i = 0; i < n; i++) cin >> bn[i];
  
  int t; cin >> t;
  char P, Q; int x, y; cin >> P >> x >> Q >> y;
  

  return 0;
}